// 3) Crie um script para exibir um número (dígito) pertencente ao intervalo [0,9] no console
// do navegador

var numAleatoria = parseInt(Math.random() * 10);

document.write("<h1>" + numAleatoria + "</h1>")